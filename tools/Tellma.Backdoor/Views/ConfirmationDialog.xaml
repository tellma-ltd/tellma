<Window x:Class="Tellma.Backdoor.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Execution"
        Width="420" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,10"
                   Text="This will execute the script on all tenant databases."/>

        <TextBlock Grid.Row="1" Margin="0,0,0,6"
                   Text="Type &quot;Confirmed&quot; below to proceed:"/>

        <TextBox Grid.Row="2" x:Name="ConfirmTextBox"
                 VerticalAlignment="Center" FontSize="14" Padding="4"
                 TextChanged="ConfirmTextBox_TextChanged"
                 KeyDown="ConfirmTextBox_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="OkButton" Content="OK" Width="80" IsEnabled="False"
                    Click="OkButton_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
