namespace Tellma.Model.Common
{
    /// <summary>
    /// The type of a property that maps to a SQL Server Geography column. Such properties are never
    /// populated (it is not supported in .NET Core), but they can be used for filtering and orderby etc...
    /// </summary>
    public sealed class Geography
    {
    }
}
