namespace Tellma.Model.Common
{
    /// <summary>
    /// A simple enum to distinguish the possible type of entity keys
    /// </summary>
    public enum KeyType
    {
        Int, 
        String, 
        None
    }
}
