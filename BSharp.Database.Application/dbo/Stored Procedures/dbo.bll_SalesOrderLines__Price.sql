CREATE PROCEDURE [dbo].[bll_SalesOrderLines__Price]
-- Input the actual line (and document) as the sales rep maybe did not save the doc yet
	@Entities int = 0,
	@param2 int
	-- Execute with readonly privilige
AS

RETURN 0
